{
  "comments": [
    {
      "path": ".devcontainer/devcontainer.json",
      "content": "`cargo install just` fetches and builds the latest `just` at container creation time, making devcontainer setup non-reproducible and dependent on current crates.io state. Pin the tool version so rebuilds use the same dependency graph.",
      "suggestion_code": "\"postCreateCommand\": \"cargo install just --version 1.42.4 --locked\",",
      "existing_code": "\"postCreateCommand\": \"cargo install just\",",
      "start_line": 20,
      "end_line": 20,
      "thinking": "Devcontainer bootstrap commands should be reproducible; unpinned cargo install can change behavior or fail unexpectedly when upstream releases change."
    }
  ]
}